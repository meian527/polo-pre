fn add(a: i32, b: i32) -> i32 {
    return a + b;
}
fn main() -> i32;
fn print(s: str) -> void {
     vmcall!(1, 1, s, 8);
}
fn main() -> i32 {
    let x: i32 = 5;
    let y: i32 = 15;
    let result: i32 = add(x, y);
    print("Hellosxs\n");
    return result;
}