pub struct Point {
    x: i32;
    y: i32;
} impl {
    constructor(self: *Self, x: i32, y: i32) {
        self.x = x;
        self.y = y;
    }

    fn distance_from_origin(self: *Self) -> f64 {
        return ((self.x * self.x + self.y * self.y) as f64).sqrt();
    }
}

pub enum Color {
    Red,
    Green,
    Blue,
    RGB(i32, i32, i32),
}

pub trait Drawable {
    fn draw(self: *Self) -> void;
}

impl Point : Drawable {
    fn draw(self: *Self) -> void {
        // 实现绘制逻辑
    }
}
